[project]
name = "organizador-estudos"
version = "0.1.0"
description = "Organizador de estudos CLI com quiz integrado via Open Trivia DB"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["requests"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100